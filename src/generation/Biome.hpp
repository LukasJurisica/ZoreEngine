#include "voxel/Chunk.hpp"

namespace zore {

	class BiomeManager {
	public:
		static void Init(int seed);
		static void GenerateBiomeMap(ubyte* biomeMap, Chunk* chunk);

	private:

	};

	class Biome {

	};
}